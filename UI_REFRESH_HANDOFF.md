# FocusBoard UI刷新 引き継ぎ

## 現在地

- リポジトリ: `/home/ishida/FocusBoard`
- 現在のブランチ: `ui-refresh`
- 直前の実装ブランチ: `opaque-accent-design`
- 作業は未コミットの状態
- `refero` skill は前セッションでは利用可能スキル一覧・ローカル配置のどちらにも露出していなかった。次セッションで利用可能なら、必ず先に `SKILL.md` を読み、プランニングに使うこと。

## ユーザーの要求

Referoの対象スタイルとAudio-Technica CONNECTを参考に、FocusBoardのUIを完全に作り直す。

- 時計と円形タイマーという大まかな構成は維持する
- それ以外のUIの形・レイアウト・カード構造は大規模に変更してよい
- 既存の基本機能と操作目的は維持する
- 不透明な白基調を基本にする。ガラス表現は使わない
- UI差し色の二重化・競合をなくす
- ユーザーが各画面でどこから操作を開始し、何を実行し、どこで終了／戻るのかを明確にする
- UIだけでなく、状態遷移や操作性などの動作も改善する
- スクリーンショットで厳しく評価し、必要なら作り直す
- 評価には設計思想を語るサブエージェントではなく、見た目と操作上の問題を具体的に指摘する厳格な評価役を使う

参考URL:

- https://styles.refero.design/style/0fd67ec5-7e9c-4ca9-b368-5d9c7388477a
- https://apps.apple.com/jp/app/audio-technica-connect/id1410663056

## 前セッションで分かった問題

前回の見た目変更は「色を変えた旧UI」に近く、ユーザーから改善不足と判断された。

厳格評価で指摘された内容:

- カード内にカードが多く、設定画面が箱の集合に見える
- 角丸が大きく、UIの階層が弱い
- 差し色が複数存在し、設定画面のトグルやタイマー色が競合していた
- 画面ごとの開始・終了導線が弱い
- 780×500程度では設定画面の情報が見切れやすい
- 背景・タイマー・設定パネルの主従が不明確
- スクリーンショット環境では日本語が読みにくく見えた。アクセシビリティスナップショット上の日本語ラベルは正常だったため、フォント環境も再確認する

## 現在入っている変更

主な変更ファイル:

- `src/index.css`: 不透明白UI、小さめの角丸、設定のフラット行化など。ただし約1500行の追加上書きがあり、今回の作り直しでは整理・置換を検討する
- `src/App.tsx`: `--accent`、`--accent-soft`、`--accent-contrast` をアプリ単位で適用
- `src/components/SettingsPanel.tsx`: UI差し色の設定を追加
- `src/types/settings.ts`: `uiAccentColor` と5色のプリセットを追加
- `src/utils/storage.ts`: UI差し色の保存・検証を追加
- `src/utils/adaptiveColor.ts`: Hexから読みやすい文字色を決める処理を追加
- `src/App.test.tsx`、`src/utils/storage.test.ts`: UI差し色の回帰テストを追加
- `src/components/tasks/TaskDrawer.test.tsx`: 日付固定による既存テストの不安定さを修正

前回の確認結果:

- `npm test -- --run`: 252 tests passed
- `npm run build`: 成功
- ブラウザ操作: タスク→設定→表示→差し色変更→再読み込み後の保持を確認
- ブラウザコンソール: エラー0件。既存の `apple-mobile-web-app-capable` deprecated 警告が1件
- 比較用画像: `/home/ishida/FocusBoard/focusboard-final-accent.png`

## 次セッションの推奨プラン

### 1. 先に画面別の操作フローを定義する

実装前に次を短く整理する:

- ホーム: 時計を見る → 円形タイマーを開始／再開／一時停止 → 終了 → ホームへ戻る
- タイマー設定: 方式を選ぶ → 時間を選ぶ → タスクを選ぶ／追加する／タスクなし → 開始
- タイマー実行中: 残り時間を確認 → 一時停止／再開 → 設定へ戻る → リセット／終了
- タスク画面: 表示先を選ぶ → タスクを追加／選択 → 編集 → 完了／アーカイブ／削除 → 一覧へ戻る
- 設定画面: カテゴリーを選ぶ → 変更 → 自動保存状態を確認 → 閉じる。低頻度設定は詳細行へ収納
- セッション完了: 結果を確認 → タスク完了／次を開始／閉じる

### 2. DOMを確認して、CSS上書きだけで済ませない

`src/App.tsx`、`src/components/PomodoroTimer.tsx`、`src/components/ClockWidget.tsx`、`src/components/tasks/TaskLauncher.tsx`、`src/components/tasks/TaskDrawer.tsx`、`src/components/SettingsPanel.tsx`、`src/components/SessionCompleteDialog.tsx` を確認する。

今回の目的は見た目の色変更ではないため、必要なら以下をコンポーネント単位で作り直す:

- ホーム上のタイマー設定ブロック
- 実行中の円形タイマー周辺の操作
- タスクランチャー
- 設定ドロワーのヘッダー／タブ／詳細行
- タスクドロワーの一覧／編集画面
- セッション完了ダイアログ

ボタンの意味・既存の役割は維持し、ボタンを増やしすぎないこと。

### 3. 差し色を一つの経路に統一する

- UIの選択状態・主要操作・進捗だけに `--accent` を使う
- 時計の色、タイマーの色、タスクテーマ色は用途を分離する
- 設定画面のトグル、スライダー、タブ、主要ボタンが旧 `--blue` や `--task-primary` に戻らないこと
- 赤は破壊的操作、警告など意味のある状態だけに限定する
- `color-mix()` を使う場合も、基準色は `--accent` だけにする

### 4. 角丸・面・余白を再設計する

現状の大きな角丸とカード入れ子を減らす。目安:

- ページ面: 0〜12px
- ドロワー／主要コンテナ: 12〜16px
- ボタン／入力: 6〜10px
- トグル／バッジのみ完全なピル
- 設定行: カード化せず、区切り線で整理

### 5. スクリーンショット評価を必ず行う

最低限、以下を確認する:

- 1180×760: ホーム、タイマー設定、タイマー実行中、タスク画面、設定各タブ
- 780×493: 同じ画面で主要ボタンと見出しが見切れないこと
- 表示・タイマー・タスク・データの各操作がアクセシビリティスナップショットで追えること
- 画面を開いた後の主操作、完了後の戻り先、キャンセル操作が明確なこと
- 参考サイトの白基調・不透明面・細い境界線と、Audio-Technica CONNECTの機器設定画面の整理感に近づいていること

## 実装時の注意

- `endAt` と `Date.now()` 基準のタイマー進行を壊さない
- localStorage / IndexedDB / AudioContext / Service Workerの失敗時も起動できる状態を保つ
- 既存のlocalStorageデータを壊さない。`uiRevision`、設定version、タイマーversionを変更する場合は移行とテストを追加
- iPad縦横、iPhone、PC、safe area、タッチ操作、キーボード操作を確認する
- `npm test`、`npm run build`、必要に応じて `npm run lint` を最後に実行する
- 無関係な変更を混ぜない。作業前後に `git status --short` を確認する

