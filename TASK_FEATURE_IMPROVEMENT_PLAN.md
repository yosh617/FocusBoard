# FocusBoard タスク機能 実装監査・改善計画

作成日: 2026-07-18  
対象ブランチ: `feat/task-features`  
対象計画: `TASK_FEATURE_PLAN.md`  
結論: 主要機能は実装済み。ただし、元計画を厳密に「すべて完了」とするには未実装項目と未検証条件が残る。

実装進捗: 最優先改善の初期実装を開始済み。追加のみ、スマートマージ、完全置換、変更件数プレビュー、競合の端末／ファイル優先、完全置換transaction、親子循環検出、反映直後のメモリ内Undo、Safariを考慮したObject URL破棄の遅延まで実装した。再起動後も使える永続スナップショット、競合ごとの個別選択、Backup v2 migrationは次段階。

初期実装の検証:

- Vitest: 19ファイル・87テスト成功
- TypeScriptを含むproduction build成功
- 375px実ブラウザで横はみ出しなし、表示中の操作領域は44px以上
- 実IndexedDBで「競合をファイル優先にして更新＋追加」を確認
- 実IndexedDBで「空バックアップによる完全置換後に3storeが0件」を確認

## 1. 監査結果の要約

タスク管理、タイマー連携、繰り返し、サブタスク、レポート、JSONバックアップという主要な利用フローは動作する。監査開始時点のJSON復元は、同じIDを上書きし、新しいIDを追加し、端末だけにあるデータを残す簡易マージだった。今回の初期実装で、安全な3方式と事前プレビューへ拡張した。

一方で、元計画に書かれた細かなUI要件、実機・大量データ検証、競合ごとの個別選択と永続ロールバックは未完了である。したがって現状は「主要実装完了・受入差分あり」と評価する。

| Phase | 評価 | 主な根拠・残り |
|---|---|---|
| 1. 保存基盤とCRUD | 主要実装済み | 型、検証、IndexedDB、CRUD、保存失敗時のフォールバックは実装済み。IndexedDBラッパー自体の自動CRUD・transactionテストは未追加 |
| 2. ドロワーとスマートリスト | 一部未達 | 一覧、プロジェクト、レスポンシブ、ARIAは実装済み。完了済みの日付グループ化、操作後トースト内のUndo、プロジェクト処理方法の選択は未実装 |
| 3. タイマー連携 | 一部未達 | タスク開始、履歴、再読込、終了ダイアログは実装済み。フローティングタイマー上のタスク名から簡易詳細を開く操作は未実装 |
| 4. 期限・繰り返し・詳細 | 主要実装済み | 期限、リマインダー、毎日・平日・毎週・毎月・カスタム周期、サブタスク、メモ、検索を実装済み |
| 5. 履歴とレポート | 主要実装済み | 日・週・月、プロジェクト、タスク、履歴、空状態を実装済み。実行環境のTZを切り替える自動テストは未追加 |
| 6. バックアップとQA | 一部未達 | JSON書き出し・復元、検証、3方式のマージ、件数・競合表示は実装済み。実機iPad、実500件データ、復元UIのE2E相当検証は未完了 |

## 2. インポート・エクスポート・マージの監査と改善状況

この節の「監査開始時」は改善前の状態を示す。現在は、その下の「今回解消した項目」まで実装済みである。

### 監査開始時に実装済みだった内容

- タスク、プロジェクト、集中履歴をversion付きJSONへ書き出す。
- 読み込みファイルを20MB以下に制限する。
- JSON形式、バックアップversion、各レコード、ID重複、親タスク・プロジェクト参照を検証する。
- 復元前にタスク、プロジェクト、履歴の件数を表示する。
- 同じIDはインポート側で上書きする。
- 新しいIDは追加する。
- 端末側にだけあるIDは残す。
- 複数storeへの書き込みは、対象storeを含む1つのIndexedDB transactionで行う。
- 不正ファイルの場合は保存処理を開始しない。

関連実装:

- `src/utils/productivityBackup.ts`
- `src/components/tasks/ProductivityBackupPanel.tsx`
- `src/hooks/useTasks.ts`
- `src/utils/productivityStorage.ts`

### 監査開始時に不足していた点

1. 同じIDなら更新日時を比較せず、古いバックアップでも端末の新しいタスクを上書きする。
2. 追加のみ、スマートマージ、完全置換を選べない。
3. 「追加」「更新」「同一」「競合」「除外」の内訳が復元前に分からない。
4. 競合しているタイトル、期限、状態などの差分を比較できない。
5. 復元後にワンタップで元へ戻す自動スナップショットがない。
6. 完全置換用のstore全削除がなく、空の配列を復元しても既存データは残る。
7. 親子関係の循環参照を検出しない。自己参照は拒否するが、A→B→Aのような循環は通る。
8. 20MB制限以外の最大レコード数、最大階層、処理時間ガードがない。
9. 将来Backup v2へ進める際に、既存のv1ファイルをv2相当へ読み替えるmigration経路がまだない。
10. ファイル選択、書き出し、競合、失敗、ロールバックを通したコンポーネント／実ブラウザテストがない。
11. `URL.revokeObjectURL()`をクリック直後に実行しており、iPad Safariではダウンロード完了後へ遅延させる方が安全である。

### 今回解消した項目

- 1〜4: 追加のみ、スマートマージ、完全置換、件数プレビュー、競合選択を追加した。
- 5: インポート直前の3storeをメモリ内に保持し、直後のUndoで原子的に戻せるようにした。再起動後も使える永続スナップショットは未実装。
- 6: 3storeを同じtransactionでclear／putする完全置換を追加した。
- 7: バックアップ単体と、端末データとのマージ結果の両方で親子循環を拒否する。
- 10: 差分解析、冪等性、Undo、方式選択、競合、完全置換確認、循環ブロックの自動テストを追加した。実ブラウザ検証は未完了。
- 11: Object URLの破棄をクリック後1秒へ遅延した。
- 8〜9: 大量データの処理ガードとBackup v2 migrationは次段階に残す。

## 3. 最優先改善: 安全なインポート・エクスポート・マージ v2

想定期間: 1〜2週間

### 3.1 3つの復元方式

| 方式 | 動作 | 用途 |
|---|---|---|
| 追加のみ | 既存IDを一切変更せず、新規IDだけ追加 | 他の端末から安全に足す |
| スマートマージ | `updatedAt`が新しいタスク／プロジェクトを採用し、判断できないものだけ競合にする | 通常の推奨方式 |
| 完全置換 | 現在のタスク、プロジェクト、履歴を消し、バックアップと完全に同じ状態へする | 故障・初期化後の完全復旧 |

集中履歴は原則不変データとして扱う。同じセッションIDで内容も同じならスキップし、内容が違う場合は自動上書きせず競合とする。

### 3.2 復元ウィザード

1. JSONファイルを選ぶ。
2. 形式、version、件数、関連付け、循環参照、上限を検証する。
3. 復元方式を選ぶ。既定値は「スマートマージ」とする。
4. 次の内訳を表示する。
   - 新規追加
   - 更新
   - 内容が同じためスキップ
   - 競合
   - 不正または参照切れで除外
   - 完全置換時に削除される端末側データ
5. 競合だけを一覧で比較し、「端末を残す」「バックアップを採用」を選ぶ。
6. 復元直前の状態を端末内に自動スナップショットとして1世代保存する。
7. 1つのtransactionで反映する。
8. 結果件数と警告を表示し、「復元前に戻す」を提示する。

ブラウザ標準の`confirm`だけに依存せず、フォーカストラップ、明確な見出し、44px以上の操作領域を持つダイアログにする。件数や警告は色だけで伝えない。

### 3.3 データ処理の構成

UIから分離した純粋関数として次を用意する。

```ts
type ImportStrategy = "add-only" | "smart-merge" | "replace";

type ImportPlan<T> = {
  inserts: T[];
  updates: T[];
  unchanged: T[];
  conflicts: ImportConflict<T>[];
  deletions: T[];
};

function analyzeProductivityImport(
  current: ProductivityData,
  incoming: ProductivityBackup,
  strategy: ImportStrategy
): ProductivityImportPlan;
```

保存層には次を追加する。

- `replaceProductivityData`: 3つのstoreを同じtransaction内でclearしてputする。
- `applyProductivityImportPlan`: 追加・更新・削除を同じtransaction内で反映する。
- `saveImportSnapshot`／`restoreImportSnapshot`: 直前1世代の復元用。
- DB version 2へのmigrationと既存データ保持テスト。

### 3.4 Backup v2

個人や端末を識別する情報は追加せず、次だけを持たせる。

- backup format version
- アプリのデータschema version
- 書き出し日時
- storeごとの件数
- タスク、プロジェクト、履歴
- 任意の設定データ。ただし進行中タイマーは既定で除外する

背景画像は容量が大きいため、通常のJSONバックアップには含めない。将来、画像込みの完全バックアップを追加する場合は、サイズ見積もりと保存容量警告を先に表示する。

### 3.5 完了条件

- 同じファイルを2回スマートマージしても2回目はすべて同一扱いになり、件数が増えない。
- 古いバックアップが新しい端末データを無断で上書きしない。
- 追加のみでは既存レコードが1件も変わらない。
- 完全置換後は、0件のstoreを含めバックアップと件数・関連付けが一致する。
- 不正、参照切れ、循環参照、上限超過ではデータを変更しない。
- transaction失敗時に一部storeだけ変更されない。
- 復元前スナップショットから元の件数と内容へ戻せる。
- VoiceOverとキーボードだけで方式選択、競合解決、復元、取消ができる。

## 4. 元計画の未達項目を閉じる改善

想定期間: 3〜5日

### 必須

1. 完了済みタスクを`completedAt`のローカル日付でグループ化し、日付ごとに折りたためるようにする。
2. 完了・アーカイブ直後のトーストに「元に戻す」操作を出す。ドロワーヘッダーのUndoは補助として残す。
3. プロジェクトのアーカイブ時に「所属タスクをInboxへ移す」「所属タスクもアーカイブ」を選べるようにする。
4. フローティングタイマーのタスク名をボタンにし、タイマーを止めずにタイトル、メモ、進捗を確認できる簡易詳細を開く。
5. タスクランチャーを無操作時に半透明へする。ただし完全には隠さず、フォーカス中は常に明瞭に表示する。
6. `productivityStorage.ts`のIndexedDB schema、put、atomic transaction、失敗時abortを自動テストする。
7. `TZ=Asia/Tokyo`、`TZ=UTC`、夏時間のあるtimezoneで日・週・月境界テストを実行する。
8. 500件のタスクと長期履歴を実データとして投入し、追加、検索、一覧、レポートの応答時間とメモリを記録する。
9. iPad Safariの縦横で、ファイル書き出し・読み込み、通知、safe area、PWA再開、VoiceOver操作を確認する。

### 計画書の表現修正

上記を完了するまでは、`TASK_FEATURE_PLAN.md`の状態を「主要実装完了・受入差分あり」とする。実装済みと実機確認済みを分けて記録する。

## 5. 次に効果が高いタスク機能改善

### 優先度A: 日常利用の摩擦を減らす（1〜2週間）

- 全リスト横断検索と、現在のリスト内検索を切り替えられるようにする。
- 期限、プロジェクト、完了状態、見積もり数で複合絞り込みを行う。
- タスク複製とテンプレートを追加する。
- 複数選択、一括完了、一括移動、一括アーカイブを追加する。
- 繰り返しの曜日を複数選択できるようにする。
- リマインダーへ「5分後」「明日」「完了」の操作を追加し、通知済み状態を端末内保存する。
- タスク追加時に期限と見積もりだけをキーボード／タッチで素早く指定できるクイック入力を追加する。

### 優先度B: レポートを意思決定に使えるようにする（1週間）

- 前日／翌日、前週／翌週、前月／翌月へ移動できるようにする。
- 見積もりと実績の差、完了率、中断率を表示する。
- プロジェクトごとの週間目標と進捗を追加する。
- 集中履歴をCSVへ書き出せるようにする。
- 「実集中時間は中断セッションの実施分も含む」など、指標定義を画面内で確認できるようにする。

### 優先度C: 大量データと保守性（1週間）

- `TaskDrawer.tsx`からNavigation、List、Row、Editorを分離する。
- レポートとバックアップ画面を`React.lazy`で遅延読み込みする。
- 500件以上では一覧を段階描画またはwindowingする。
- 検索入力へ`useDeferredValue`を使い、入力応答を維持する。
- IndexedDBの`getAll()`だけに依存せず、status、dueDate、projectId、startedAt indexを使った範囲取得を追加する。
- 長期履歴は50件固定表示ではなく、ページングまたは「さらに表示」にする。

## 6. 低優先度の候補

Focus To-Do公式情報では、期限、リマインダー、サブタスク、繰り返し、メモ、レポートが主要機能として案内されている。これらはFocusBoardでも対応済みである。公式プライバシー情報には優先度、タグ、期間指定、見積もり／実績もタスクデータ例として記載されているため、次の候補にできる。

- 優先度
- タグ
- 開始日と終了日の期間指定
- 試験日までの残日数と必要学習時間
- 完了済みタスクの自動アーカイブ期間

タグや優先度は入力項目を増やすため、まず検索、テンプレート、一括操作の利用性を確認してから追加する。他アプリ・Webサイトのブロック、クラウド同期、共有機能は引き続き対象外とする。

## 7. 改善後のテスト計画

### Import／Export utility

- v1からv2へのmigration
- 追加のみ、スマートマージ、完全置換
- 新旧`updatedAt`、同時刻で内容違い、同一内容
- 同一セッションIDで内容違い
- 親子循環、参照切れ、重複ID、上限超過
- 0件storeと500件以上のデータ
- 同一ファイルの複数回適用

### IndexedDB

- 3 storeへのatomic put
- clearとputを含む完全置換
- 途中失敗時のabort
- version 1から2へのmigration
- import snapshotからのrollback

### Components

- ファイル選択後の件数・競合プレビュー
- 方式選択と競合解決
- 復元中の二重実行防止
- 成功、失敗、取消、復元前に戻す
- フォーカストラップと実行後のフォーカス位置

### 実ブラウザ

- iPad SafariでJSONの保存とFilesからの選択
- 375px、768px、1024px、1440pxの横スクロール
- 500タスク、10,000履歴での検索・レポート・マージ時間
- オフライン状態での書き出し、復元、再読み込み

## 8. 推奨実装順

1. 元計画の状態表記を修正する。
2. import分析を純粋関数として実装し、テストを先に作る。
3. IndexedDBの完全置換、削除、rollback transactionを実装する。
4. 復元ウィザードと競合UIを追加する。
5. iPad Safariでexport／importを確認する。
6. 元計画の残項目を閉じる。
7. 全リスト検索、テンプレート、一括操作へ進む。
8. 過去期間レポート、CSV、週間目標を追加する。

## 9. 参考情報

- [Focus To-Do公式サイト](https://www.focustodo.cn/?lang=en_US)
- [Focus To-Do Privacy Policy](https://www.focustodo.cn/privacy-policy)
- `TASK_FEATURE_PLAN.md`
- `README.md`

## 10. 改善書レビュー結果

レビュー日: 2026-07-18  
判定: 実装着手可能

### 維持する判断

- ローカル完結、外部送信なし、追加依存なしを基本にする。
- 通常操作の既定値はスマートマージとし、完全置換は明確に分離する。
- UIを作る前に純粋な差分解析と受入テストを作る。
- 同じファイルを複数回適用しても増殖しないことを必須条件にする。
- 競合と削除予定は色だけでなく件数と文字で示す。

### 実装時に補正する点

- `updatedAt`は端末時計の差に影響されるため、スマートマージでも更新予定をプレビューし、ユーザーが実行前に確認できるようにする。
- Backup v1は既存利用者のため読み込みを維持する。マージ方式の追加だけを理由にファイルversionは上げない。
- DB version 2は永続Undoスナップショットstoreを追加するときに上げ、migrationテストと同じ変更に含める。
- 最初の実装では直前データをメモリ上に保持してUndo可能にし、永続スナップショットは次の小段階で追加する。
- 背景画像はサイズとSafariのファイル処理リスクが大きいため、生産性データのマージには含めない。

### 今回着手する範囲

1. `productivityImport.ts`の純粋な差分解析
2. 追加のみ、スマートマージ、完全置換
3. 追加、更新、維持、同一、競合、削除の事前プレビュー
4. 競合時の「端末を維持／バックアップを採用」選択
5. 3storeを原子的に完全置換する保存API
6. インポート直後のUndo
7. 親子循環参照の拒否
8. Safariを考慮したObject URL破棄の遅延

この範囲が完了した後、永続Undoスナップショット、Backup v2、元計画のUI残項目へ進む。
