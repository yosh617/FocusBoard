# FocusBoard タスク機能実装計画

作成日: 2026-07-18  
想定期間: 12〜14週間（1人開発、2週間単位）  
状態: 主要実装完了・受入差分あり（`TASK_FEATURE_IMPROVEMENT_PLAN.md`参照）

## 1. 目的

FocusBoardに、タスクを選んで集中を始め、実績を振り返れる一連の流れを追加する。

目標とする体験は次のとおり。

1. 今日取り組むタスクを迷わず選べる。
2. タスクからポモドーロまたは通常タイマーを開始できる。
3. 集中時間と完了したポモドーロ数がタスクへ自動記録される。
4. 期限、繰り返し、サブタスク、メモを端末内で管理できる。
5. 日・週・月単位で、タスクやプロジェクトごとの集中実績を確認できる。

既存の「背景を主役にした静かな集中画面」は維持し、常時大きなタスクリストを表示しない。タスク管理は必要なときだけ開き、集中開始後は選択中のタスク名だけを小さく表示する。

## 2. 参考にするFocus To-Doの機能

[Focus To-Do公式サイト](https://www.focustodo.cn/)で案内されているタスク関連機能を、FocusBoardのローカル完結方針に合わせて採用する。

| Focus To-Doの要素 | FocusBoardでの対応 |
|---|---|
| タスクとポモドーロの統合 | タスク行または詳細画面からタイマーを開始する |
| Inbox／Today／Tomorrow／Upcoming／Someday | スマートリストとして用意する |
| プロジェクト | 科目、仕事、個人などの分類として用意する |
| 期限 | 日付と期限切れ表示を用意する |
| リマインダー | アプリ内表示を基本とし、利用可能な環境だけシステム通知を補助的に使う |
| 繰り返し | 毎日、平日、毎週、毎月、カスタム周期に対応する |
| サブタスク | タスク詳細内のチェックリストとして用意する |
| メモ | プレーンテキストのメモを用意する |
| 見積もりポモドーロ数 | タスクごとに予定セッション数を設定する |
| 履歴・統計 | 日／週／月、プロジェクト別、タスク別の集中時間を表示する |

### 対象外

- アプリ、Webサイト、通知などのブロック機能
- アカウント、クラウド同期、複数端末同期
- 他ユーザーとの共有、共同編集
- 外部カレンダーや外部タスクサービスとの連携
- チャット、SNS、ランキングなど集中を妨げる機能
- 位置情報、連絡先、端末内画像の外部送信

## 3. 情報設計

### 3.1 スマートリスト

- **Inbox**: プロジェクトや期限を決めずに追加したタスク
- **今日**: 今日が期限のタスクと期限切れタスク
- **明日**: 明日が期限のタスク
- **今後**: 明後日以降の期限があるタスク
- **いつか**: 期限を決めず、Inboxから明示的に移したタスク
- **完了済み**: 完了日単位で折りたたんで表示

期限切れは独立画面にせず、「今日」の先頭へ表示する。色だけでなく「期限切れ」の文字とアイコンでも状態を伝える。

### 3.2 プロジェクト

- 初期状態ではプロジェクトなしでも利用可能にする。
- ユーザーはプロジェクト名と色を設定できる。
- プロジェクトの削除時は、所属タスクをInboxへ移すか同時にアーカイブするか確認する。
- 集中履歴にはプロジェクト名のスナップショットを保持し、後から名称変更や削除をしても過去の集計を維持する。

### 3.3 タスク

必須項目はタイトルだけとし、入力開始から保存までを短くする。詳細設定はタスク選択後に開く。

- タイトル
- 完了状態
- プロジェクト
- 期限
- リマインダー
- 繰り返し
- 見積もりポモドーロ数
- サブタスク
- メモ
- 並び順
- 作成・更新・完了日時

タスク削除は即時の物理削除にせず、まずアーカイブ扱いにする。完了や削除の直後はトーストから取り消せるようにする。

## 4. 画面と操作

### 4.1 タスクランチャー

- 画面左端へ「今日 3」のようなコンパクトなピル型ボタンを置く。
- 最小44×44pxの操作領域を確保する。
- 一定時間操作がなければ半透明になるが、完全には消さない。
- キーボード利用時は常にフォーカス可能にする。

### 4.2 タスクドロワー

- iPad横向きでは左から幅380〜440pxで開く。
- iPad縦向きと小画面では画面幅のほぼ全体を使う。
- 上部にスマートリスト、下部にプロジェクトを配置する。
- メイン領域にはタスク一覧、選択したタスクの詳細を表示する。
- ダイアログ相当のARIA、Escapeで閉じる操作、フォーカス復帰を用意する。
- 主要操作を横スワイプだけに依存させない。

### 4.3 タスク行

タスク行には次を表示する。

- 完了チェック
- タイトル
- 期限または「期限切れ」
- プロジェクト色と名称
- 見積もり数と完了セッション数（例: `2 / 4`）
- タイマー開始ボタン

行全体で詳細を開き、開始ボタンは別操作にする。チェック、詳細、開始のタップ領域が重ならないよう8px以上離す。

### 4.4 集中開始後

- ドロワーを自動的に閉じる。
- フローティングタイマー内または直下へ、選択中タスク名を1〜2行で表示する。
- タスク名をタップすると、タイマーを停止せず簡易詳細を確認できる。
- 作業セッション完了時だけ実績へ加算し、休憩はタスク実績に加算しない。
- 中断したセッションも履歴には残すが、完了ポモドーロ数には含めない。

### 4.5 セッション終了

作業終了時に次の操作を提示する。

- 次の休憩へ進む
- 同じタスクをもう1セッション続ける
- タスクを完了する
- タスクは未完了のまま終了する

完了処理は再読み込み後に重複加算されないよう、セッションID単位で冪等にする。

## 5. データ設計

タスク、プロジェクト、集中履歴は件数が増えるためIndexedDBへ保存する。設定と進行中タイマーは引き続きlocalStorageを使う。

### 5.1 IndexedDB

データベース名: `focusboard-productivity`  
初期スキーマバージョン: `1`

#### `tasks`

```ts
type TaskRecord = {
  version: 1;
  id: string;
  title: string;
  status: "open" | "completed" | "archived";
  bucket: "inbox" | "someday";
  projectId: string | null;
  parentTaskId: string | null;
  note: string;
  dueDate: string | null; // ローカル日付の YYYY-MM-DD
  reminderAt: number | null;
  repeatRule: RepeatRule | null;
  repeatSeriesId: string | null;
  estimatedPomodoros: number;
  order: number;
  createdAt: number;
  updatedAt: number;
  completedAt: number | null;
};
```

#### `projects`

```ts
type ProjectRecord = {
  version: 1;
  id: string;
  name: string;
  color: string;
  order: number;
  archivedAt: number | null;
  createdAt: number;
  updatedAt: number;
};
```

#### `focusSessions`

```ts
type FocusSessionRecord = {
  version: 1;
  id: string;
  taskId: string | null;
  taskTitleSnapshot: string | null;
  projectIdSnapshot: string | null;
  projectNameSnapshot: string | null;
  program: "pomodoro" | "countdown" | "countup";
  mode: "work" | "shortBreak" | "longBreak";
  result: "completed" | "cancelled";
  startedAt: number;
  endedAt: number;
  plannedDurationMs: number;
  focusedDurationMs: number;
};
```

### 5.2 インデックス

- `tasks`: `status`, `projectId`, `dueDate`, `completedAt`, `parentTaskId`
- `projects`: `order`, `archivedAt`
- `focusSessions`: `taskId`, `startedAt`, `projectIdSnapshot`, `[taskId, startedAt]`

### 5.3 タイマー状態の移行

`TimerState.version`を`3`へ上げ、次を追加する。

- `activeTaskId: string | null`
- `activeSessionId: string | null`
- `sessionStartedAt: number | null`

version 1／2からの移行ではすべて`null`を設定し、既存タイマーの残り時間と`endAt`を維持する。移行処理と回帰テストを同じ変更に含める。

### 5.4 保存データの扱い

- IndexedDBから取得した値は信用せず、文字数、数値範囲、日付形式、ID形式を検証する。
- 壊れた1件のために一覧全体を起動不能にしない。無効レコードを除外し、ユーザーへ件数を通知する。
- タイトルは1〜200文字、メモは最大10,000文字、見積もりは0〜99に制限する。
- 繰り返しタスクは完了時に次回分を1件だけ生成し、`repeatSeriesId`で重複を防ぐ。
- 端末のタイムゾーン変更で期限日がずれないよう、期限は時刻ではなくローカル日付文字列で保存する。

## 6. コード構成

`App.tsx`へ処理を集中させず、次の責務に分ける。

```text
src/
  components/tasks/
    TaskLauncher.tsx
    TaskDrawer.tsx
    TaskNavigation.tsx
    TaskList.tsx
    TaskRow.tsx
    TaskEditor.tsx
    ActiveTaskChip.tsx
    SessionCompleteDialog.tsx
    FocusReport.tsx
  hooks/
    useTasks.ts
    useProjects.ts
    useFocusSessions.ts
    useTaskReminder.ts
  types/
    task.ts
    project.ts
    focusSession.ts
  utils/
    productivityStorage.ts
    taskValidation.ts
    taskQueries.ts
    repeatRule.ts
    focusReport.ts
```

- UIコンポーネントは保存APIを直接呼ばず、hooks経由で操作する。
- スマートリスト抽出、繰り返し計算、集計は純粋関数にする。
- `usePomodoroTimer`はタイマー進行を担当し、履歴確定は専用のセッション処理へ委譲する。
- IndexedDBや通知が利用できなくても、時計とタイマーだけは起動できる状態を守る。

## 7. 実装フェーズ

### Phase 1: 保存基盤と基本CRUD（1〜2週目）

進捗: 主要実装完了（IndexedDBラッパーの直接テストは未達）

実装内容:

- 型、IndexedDBラッパー、検証、初期スキーマ
- タスクとプロジェクトの追加、編集、完了、アーカイブ
- `useTasks`と`useProjects`
- 保存失敗時のフォールバック表示
- pure utilityとIndexedDBのテスト

完了条件:

- オフラインでタスクとプロジェクトを作成・再読込できる。
- 不正データがあってもアプリが起動する。
- 完了・アーカイブを取り消せる。

### Phase 2: タスクドロワーとスマートリスト（3〜4週目）

進捗: 主要実装完了（完了日のグループ化など受入差分あり）

実装内容:

- タスクランチャー、左ドロワー、タスク一覧、詳細編集
- Inbox、今日、明日、今後、いつか、完了済み
- プロジェクト別一覧、並べ替え、件数表示
- iPad縦横、iPhone、PC向けレスポンシブ対応
- キーボード、VoiceOver、フォーカス復帰

完了条件:

- タイトルだけで素早くタスクを追加できる。
- 期限切れを含む今日のタスクが正しく抽出される。
- タッチ操作だけで全機能を利用できる。

### Phase 3: タイマー連携とセッション履歴（5〜6週目）

進捗: 主要実装完了（選択タスクから簡易詳細を開く操作は未達）

実装内容:

- タスクからポモドーロ、カウントダウン、カウントアップを開始
- `TimerState` version 3への移行
- 選択中タスク表示
- 完了／中断セッションの保存
- 終了ダイアログと実績の重複防止
- タスクの見積もりポモドーロ数と実績表示

完了条件:

- タスクから開始した作業セッションが該当タスクへ記録される。
- 休憩はタスクの完了セッション数へ加算されない。
- 再読み込みや完了処理の再実行で実績が二重加算されない。
- タスクなしでも従来どおりタイマーを使える。

### Phase 4: 期限・繰り返し・サブタスク・メモ（7〜8週目）

進捗: 完了

実装内容:

- 期限と期限切れ表示
- アプリ内リマインダー
- 対応環境でのみシステム通知を使用するオプション
- 毎日、平日、毎週、毎月、カスタム繰り返し
- サブタスクとメモ
- 検索とプロジェクト絞り込み

完了条件:

- 繰り返しタスクの次回分が1件だけ生成される。
- 通知権限を拒否してもアプリ内リマインダーは動作する。
- サブタスクを完了しても親タスクが意図せず自動完了しない。

### Phase 5: 履歴とレポート（9〜10週目）

進捗: 主要実装完了（実行環境のタイムゾーン切替テストは未達）

実装内容:

- 今日の見積もり時間、実集中時間、残タスク数、完了数
- 日／週／月の集中時間
- プロジェクト別の時間比率
- タスク別の見積もり対実績
- 完了・中断セッションの履歴
- データが少ない場合の空状態

完了条件:

- 同じ保存データから常に同じ集計結果が得られる。
- グラフだけでなく表またはテキストでも値を確認できる。
- 日付境界とタイムゾーン変更のテストがある。

### Phase 6: バックアップ、品質、リリース準備（11〜12週目）

進捗: 一部未達（実機iPad・500件以上・PWAの受入確認が必要）

実装内容:

- タスク、プロジェクト、履歴を含むJSON書き出し・復元
- 復元前の件数プレビュー、形式検証、重複処理
- 「ローカルデータをすべて削除」の対象と説明を更新
- README更新
- 大量タスクでの表示性能確認
- 375px、768px、1024px、1440pxとiPad縦横のQA
- 全テスト、production build、PWAオフライン確認

完了条件:

- バックアップから元の件数と関連付けを復元できる。
- タスク保存領域を削除しても時計とタイマーは起動する。
- 主要操作に44×44px以上の領域と明確なフォーカス表示がある。

### バッファ（13〜14週目）

- iPad実機で見つかった問題の修正
- 保存データ移行の追加ケース
- 長いタイトル、500件以上のタスク、長期間の履歴への対応
- リリース前の回帰確認

## 8. テスト計画

### Utility

- 不正なタスク、プロジェクト、履歴の検証と除外
- Inbox／今日／明日／今後／いつかの境界値
- 月末、年末、うるう年、タイムゾーン変更
- 繰り返しルールと重複生成防止
- プロジェクト別、日／週／月集計

### Hooks

- CRUDと再読み込み相当の復元
- 保存失敗時の状態と通知
- タスク選択からタイマー開始までの遷移
- 完了、中断、リセット、休憩の履歴
- タイマー終了と履歴保存の冪等性

### Components

- role、label、textを使った追加、編集、完了、開始操作
- ドロワーのフォーカストラップとフォーカス復帰
- 期限切れが色以外でも伝わること
- タスク行と開始ボタンのタッチ領域
- レポートのテキスト代替

### E2E相当の統合確認

1. タスクを追加する。
2. 見積もりを設定する。
3. タスクからタイマーを開始する。
4. 画面非表示と再読み込みを挟む。
5. セッションを完了する。
6. タスク実績と日次レポートへ1回だけ反映されることを確認する。

## 9. 主なリスクと対策

| リスク | 対策 |
|---|---|
| タスク機能で集中画面が複雑になる | ドロワー方式とし、集中開始後はタスク名だけ表示する |
| 履歴の二重記録 | `activeSessionId`とセッションIDを使い、保存を冪等にする |
| 通知が環境により動作しない | アプリ内リマインダーを基本にし、システム通知は補助扱いにする |
| IndexedDB障害でアプリが起動しない | タスク機能だけを無効化し、時計・タイマーを継続する |
| 繰り返しタスクが大量生成される | 完了時に次回1件だけ生成する |
| タスク削除で過去レポートが壊れる | 履歴へタイトルとプロジェクト名のスナップショットを保存する |
| 機能過多で入力が遅くなる | クイック追加はタイトルだけ、詳細は後から設定する |

## 10. 初回リリース後の候補

初回12〜14週間には含めない。

- タスクテンプレート
- 学習科目ごとの週間目標
- 試験日までの残り日数と必要学習時間
- タスクの複製と一括編集
- タグ
- レポートのCSV出力
- 完了済みタスクの自動アーカイブ期間

クラウド同期や外部サービス連携を将来検討する場合も、端末内保存を既定とし、明示的な同意なしにデータを送信しない。

## 11. 実装開始時の順序

最初の実装PRでは、画面を先に広げず次の順番を守る。

1. データ型と検証ルールを確定する。
2. IndexedDBスキーマと移行テストを作る。
3. 純粋なスマートリスト抽出処理を作る。
4. hooksへCRUDを実装する。
5. 最小のInbox UIを接続する。
6. タイマー連携は基本CRUDが安定してから追加する。
