# タスク画面 UI デザイン修正要件

この文書は、会話内でユーザーが明示したデザイン修正指示のみを整理したものです。Codexによる自己評価、追加提案、計測結果、独自判断による改善内容は含みません。

## 全体方針

- Focus To-Doのような、シンプルで洗練されたUIにする。
- 余白を確保しながら、情報密度の高いタスク入力UIにする。
- 初期表示だけでなく、操作後や展開後のUIも同じデザイン品質にそろえる。
- 具体的なコンポーネント構成や詳細レイアウトは、既存UIとの統一感を踏まえて最適化する。

## ボタンとタブ

- ボタンやタブの選択状態を、強い背景色の塗りつぶしで表現しない。
- タスク管理の主要タブはアンダーラインではなく、設定タブと同じ選択表示にする。
- 大量のボタンが並んでいる印象を弱める。
- タスク追加部分では「追加」ボタンだけを主要アクションとして明確にする。
- 設定タブは狭い幅でも、アイコンとラベルを各選択領域の中央へそろえる。
- 同種の主要タブでも、選択背景と内容の中心がずれないよう中央配置を統一する。

## アプリ全体のUI原則

- Apple Human Interface Guidelinesを中心に、シンプルで自己説明的なUIへ統一する。
- 操作方法の説明文に頼らず、配置、階層、一般的なアイコン、ラベル、選択状態で理解できるようにする。
- UIを見れば分かる見出しや補足文は削除するが、意味が曖昧になるまで文字を減らさない。
- カード、枠線、バッジ、見出し、説明文を必要以上に使わず、余白、整列、文字サイズ、濃淡で階層を作る。
- 主要操作を優先し、詳細設定や低頻度操作は必要なときだけ展開する。
- タスク、レポート、設定、データ管理、モーダル、ポップアップで同種の操作の見た目、挙動、配置を統一する。
- アクセントカラーは選択状態と主要アクションなど必要な箇所に限定する。
- ブラウザ標準のcalendarやselect（`input type="date"`、`select`など）を直接使わず、共通のアプリUIコンポーネントで選択操作を実装する。

## SVG・ビジュアル

- テキストだけより理解しやすい場合は、既存UIと線幅、角、サイズを揃えたシンプルなSVGを使用する。
- 空状態、機能識別、補助表示では、説明文を自然に減らせる場合にSVGを検討する。
- 装飾目的やAI生成感の強いイラストは使用しない。
- 絵文字をUIアイコンの代用にしない。
- 独自アイコンだけで意味を伝えず、必要なラベルと組み合わせる。

## 検索

- 「タイトル・メモを検索」する検索UIを削除する。

## タスク追加UI

- 通常時はタスク名入力を主役にする。
- タスク画面の表示順は「今日などの見出し→タスクを追加入力→今日のタスク一覧→設定（予定数など）」とする。
- タスク追加の「＋」はさらに小さくし、タスク追加欄全体もさらに小さく軽くする。
- 期限、日付、集中回数などは補助情報としてコンパクトに扱う。
- 詳細設定は、必要なときだけ自然に展開できるようにする。
- 詳細展開時の大きな囲み枠、ボタン、余白を減らす。
- フォーム感が強くなりすぎない軽い見た目にする。
- 「今日」「明日」「期限なし」などの期限選択を省スペース化する。
- 「集中回数」の選択UIを省スペース化する。
- 下部フィルタを現在より軽くし、ボタンが並んでいる印象を弱める。
- 追加画面の操作を横幅いっぱいの大きなボタンにせず、中央にひとまとまりとして整列する。
- 単純な縮小ではなく、余白と整列によって軽く洗練された見た目にする。
- 予定ポモドーロ数のスライダーは黒を基調とし、追加画面全体を枠で囲わない。
- タグボタンを表示する場合は飾りや未対応状態にせず、タグの追加、選択、保存、再編集まで実装する。
- すべての操作へ一律に44px以上を求めず、用途と画面密度に合わせた寸法にする。
- 「スマートリスト」という表記を使用しない。
- タスク追加画面では、補助設定を入力欄の下部にまとめる。
- UI評価では主要状態のスクリーンショットを取得し、実際の見た目で判断する。
- タスクの編集は一覧内の展開ではなく、専用画面へ遷移して表示する。専用画面には一覧へ戻るボタンを用意する。
- 狭い画面ではリスト選択とタスク表示を同時に詰め込まず、表示先を選んでからタスク画面へ切り替える。

## プロジェクトの色選択UI

- ブラウザ標準のカラーピッカー（`input type="color"`など）を使用しない。
- アプリ側で用意した固定カラーパレットから選択する方式にする。
- 落ち着いて使いやすい色を8〜12色程度用意する。
- 色は小さな丸またはスウォッチで一覧表示する。
- 選択中の色だけが明確に分かるようにする。
- Focus To-Doのように、操作が軽くシンプルに見えるUIにする。
- OSやブラウザ標準のカラーピッカーを開かない。
- 具体的な配色、サイズ、配置は、既存UIとの統一感を見て最適化する。

## 操作状態の確認要件

- 初期表示だけを見て判断せず、実際にアプリを操作してUI全体を確認する。
- ボタン、タブ、フィルタ、詳細表示、入力フォームを実際に操作する。
- 展開後、選択後、入力中、空状態、データあり状態など、主要なUI状態を確認する。
- 各状態で、情報量、視認性、操作性、一貫性を確認する。
- 初期画面だけを整えて、操作後のUIが古いまま残る状態を避ける。
- 改善後も同じ操作を一通り行い、表示崩れや不統一がないことを再確認する。

## 作業方法に関する指定

- UIスキルを使用せず、既存アプリを調査して実行する。
- ファイル編集を並列化する場合は、`gpt-5.6-luna`のhigh reasoningを使用する。
- `gpt-5.6-sol`ではファイル編集を実施しない。

## 記録の運用

- 今後ユーザーから新しいデザイン変更の指示があった場合は、この文書へ継続して記録する。
- ユーザーが明示した指示と、Codexによる提案、推測、自己評価を区別する。
- この文書にはユーザーが明示したデザイン変更指示だけを記載し、Codexによる提案、推測、自己評価は含めない。
